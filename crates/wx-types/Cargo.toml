[package]
name = "wx-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared domain model for wxforge"

[dependencies]
serde = { version = "1", features = ["derive"] }

[lib]
path = "src/lib.rs"
